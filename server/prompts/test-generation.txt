You are creating functional test cases that simulate real user interactions.

PAGE INFO:
- URL: {{url}}
- Title: {{title}}
- Elements: {{elementsCount}}
- Forms: {{forms}}, Buttons: {{buttons}}, Inputs: {{inputs}}

NAVIGATION:
{{navigationElements}}

BUSINESS CONTEXT:
{{businessContext}}

YOUR TASK:
Create 5-10 functional test cases that real users would perform.

KEY RULES:
1. Include navigation steps FIRST if content requires clicking menu/nav items
2. Use realistic test data based on business context
3. Use specific CSS selectors from the page
4. Include clear expected results

TEST DATA EXAMPLES:
{{testDataExamples}}

EXAMPLE - Login Test:
{
  "testCases": [
    {
      "type": "functional",
      "name": "User login with valid credentials",
      "description": "User logs in with email and password",
      "steps": [
        {
          "action": "navigate",
          "selector": "",
          "value": "https://example.com/login",
          "description": "Navigate to login page"
        },
        {
          "action": "fill",
          "selector": "#email",
          "value": "{auth_username}",
          "description": "Enter email"
        },
        {
          "action": "fill",
          "selector": "#password",
          "value": "{auth_password}",
          "description": "Enter password"
        },
        {
          "action": "click",
          "selector": "#login-button",
          "value": "",
          "description": "Click login button"
        },
        {
          "action": "verify",
          "selector": ".dashboard",
          "value": "",
          "description": "Verify dashboard appears"
        }
      ],
      "expectedResult": "User is authenticated and redirected to dashboard",
      "priority": "high",
      "businessValue": "Validates core authentication flow",
      "validationCriteria": [
        "Login button responds",
        "User redirected to dashboard",
        "No error messages shown"
      ]
    }
  ]
}

ACTIONS:
- navigate: Go to URL (value = URL)
- click: Click element
- fill: Enter text (value = text to enter)
- select: Choose option (value = option value)
- wait: Wait for element
- verify: Check element exists

SPECIAL TOKENS:
- {auth_username}: Use for username/email fields
- {auth_password}: Use for password fields

Return ONLY valid JSON with test cases.
