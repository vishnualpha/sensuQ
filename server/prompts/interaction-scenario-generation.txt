You are generating test scenarios for a web page.

CONTEXT:
- URL: {url}
- Title: {title}
- Page Type: {pageType}
- Business: {businessContext}

INTERACTIVE ELEMENTS:
{interactiveElements}

YOUR TASK:
Create 3-8 realistic user interaction scenarios. Think like a real user.

RULES:
1. Use ONLY selectors from the interactive elements list above
2. Use standard CSS selectors (e.g., '#id', '.class', 'input[name="x"]')
3. NO Playwright syntax like 'button:has-text' or 'link:has-text'
4. Group related actions (e.g., fill form + submit)
5. For auth fields, use {auth_username} and {auth_password} placeholders

PRIORITY:
- high: Login, signup, checkout, search, critical navigation
- medium: Filters, settings, secondary features
- low: Cosmetic interactions, tooltips

EXAMPLE:
{
  "scenarios": [
    {
      "name": "User login",
      "description": "User logs into account",
      "priority": "high",
      "steps": [
        {
          "action": "fill",
          "elementType": "input",
          "selector": "#email",
          "textContent": "Email",
          "value": "{auth_username}",
          "expectedOutcome": "Email entered"
        },
        {
          "action": "fill",
          "elementType": "input",
          "selector": "#password",
          "textContent": "Password",
          "value": "{auth_password}",
          "expectedOutcome": "Password entered"
        },
        {
          "action": "click",
          "elementType": "button",
          "selector": "#login-btn",
          "textContent": "Sign In",
          "expectedOutcome": "User authenticated"
        }
      ],
      "expectedFinalOutcome": "User logged in successfully",
      "isTestable": true,
      "leadsToNewPage": true
    }
  ]
}

SPECIAL TOKENS:
- Use {auth_username} for username/email fields
- Use {auth_password} for password fields

Return ONLY valid JSON.
